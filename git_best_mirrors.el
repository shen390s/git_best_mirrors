;; git_best_mirrors.el --- use best mirror for china users -*- lexical-binding: t -*-

;;;###autoload
(defun git_best_mirrors ()
  (message "git_best_mirrors is ready!"))

(provide 'git_best_mirrors)
